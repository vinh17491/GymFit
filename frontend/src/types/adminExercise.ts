export interface AdminExercise { id: number; name: string; slug: string; description: string | null; instructions: string | null; muscle_group: string | null; equipment: string | null; difficulty: string | null; video_url: string | null; thumbnail_url: string | null; is_active: boolean; created_at: string; updated_at: string; }
export interface AdminExerciseInput { name: string; slug?: string; description?: string | null; instructions?: string | null; muscle_group?: string | null; equipment?: string | null; difficulty?: string | null; video_url?: string | null; thumbnail_url?: string | null; }
