import { useEffect, useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import { getCoachExercise } from '../../services/coachWorkspaceApi';
import type { CoachExercise } from '../../types/coachWorkspace';
import { CoachPage, ErrorState, LoadingState } from './CoachCommon';
export default function CoachExerciseDetailPage() { const { exerciseId } = useParams(); const [item,setItem]=useState<CoachExercise|null>(null); const [error,setError]=useState(''); useEffect(()=>{ if(exerciseId) getCoachExercise(Number(exerciseId)).then(setItem).catch(()=>setError('Exercise không tồn tại hoặc đã inactive.')); },[exerciseId]); return <CoachPage title={item?.name || 'Exercise detail'} description="Chi tiết hướng dẫn dạng text; Coach không chỉnh sửa Exercise global." backTo="/coach/exercises">{!item&&!error?<LoadingState/>:error?<ErrorState message={error}/>:item&&<div className="grid gap-6 lg:grid-cols-[1fr_1.4fr]"><div className="dashboard-panel"><p className="text-xs uppercase tracking-wider text-emerald-400">{item.muscle_group||'Exercise'}</p><h2 className="mt-2 text-2xl font-bold text-white">{item.name}</h2><p className="mt-3 text-slate-400">{item.description||'Không có mô tả.'}</p><dl className="mt-6 grid grid-cols-2 gap-4 text-sm"><div><dt className="text-slate-500">Difficulty</dt><dd className="text-white">{item.difficulty||'—'}</dd></div><div><dt className="text-slate-500">Equipment</dt><dd className="text-white">{item.equipment||'—'}</dd></div></dl></div><div className="dashboard-panel"><h2 className="text-lg font-semibold text-white">Instructions</h2><p className="mt-4 whitespace-pre-wrap text-sm leading-7 text-slate-300">{item.instructions||'Chưa có hướng dẫn text.'}</p><Link className="primary-button mt-6 inline-flex" to="/coach/workout-programs/new">Dùng trong Program</Link></div></div>}</CoachPage>; }
